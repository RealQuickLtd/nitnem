<?xml version="1.0" encoding="utf-8"?>
<dev.oneuiproject.oneui.layout.ToolbarLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/toolbarLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:expanded="true"
    app:mainRoundedCorners="none"
    app:showNavButtonAsBack="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <ProgressBar
            android:id="@+id/progressBar"
            style="@android:style/Widget.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="3dp"
            android:max="100"
            android:progress="0"
            android:progressDrawable="@drawable/progress_bar_drawable" />

        <FrameLayout
            android:id="@+id/readerContainer"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <androidx.core.widget.NestedScrollView
                android:id="@+id/scrollView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="?android:colorBackground"
                android:clipToPadding="false"
                android:fillViewport="true"
                android:overScrollMode="always">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:paddingStart="@dimen/content_padding"
                    android:paddingTop="@dimen/content_padding"
                    android:paddingEnd="@dimen/content_padding"
                    android:paddingBottom="@dimen/reader_content_bottom_padding">

                    <FrameLayout
                        android:id="@+id/resumeCardContainer"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginBottom="@dimen/resume_card_spacing"
                        android:visibility="gone" />

                    <LinearLayout
                        android:id="@+id/contentContainer"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="vertical" />

                </LinearLayout>

            </androidx.core.widget.NestedScrollView>

            <dev.oneuiproject.oneui.widget.ScrollAwareFloatingActionButton
                android:id="@+id/btnAutoScrollSpeed"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom|end"
                android:layout_margin="@dimen/reader_controls_margin"
                android:contentDescription="@string/scroll_speed_title"
                android:src="@drawable/ic_oui_speed"
                android:tint="?actionMenuTextColor"
                android:tooltipText="@string/scroll_speed_title"
                android:visibility="gone"
                app:layout_behavior="@null"
                app:fabCustomSize="@dimen/reader_control_button_size" />

        </FrameLayout>

        <dev.oneuiproject.oneui.widget.MarginsTabLayout
            android:id="@+id/sectionTabs"
            android:layout_width="match_parent"
            android:layout_height="@dimen/section_tab_height"
            android:background="@color/reader_toolbar_surface"
            android:visibility="gone"
            app:seslTabStyle="Sub"
            app:tabGravity="center"
            app:tabMode="scrollable" />

    </LinearLayout>

</dev.oneuiproject.oneui.layout.ToolbarLayout>
